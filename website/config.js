// Replace with your Lambda Function URL or API Gateway endpoint.
// The Lambda returns { actions: [...] } when called with ?view=actions.
window.SELF_HEALING_ENDPOINT = "https://YOUR-FUNCTION-URL.lambda-url.us-east-1.on.aws/";
